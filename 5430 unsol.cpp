#include <bits/stdc++.h>
using namespace std;

int main(void){
    ios::sync_with_stdio(0);
    cin.tie(0);
    int N;
    while(N--){
        string op;
        cin>>op;
        int K;
        cin>>K;        
            deque<char> dq;
            string list;
            cin>>list;
    }
}