#include <bits/stdc++.h>
using namespace std;

int main(void){
    ios::sync_with_stdio(0);
    cin.tie(0);
    int N;
    cin>>N;
    stack<int> S;
    string ans;
    for(int i=1; i<=N; ++i){
        int k;        
        cin>>k;
        while(k>i) {
            S.push(i);
            
        }
        if(k<i) {
            ans
        }
    }
}